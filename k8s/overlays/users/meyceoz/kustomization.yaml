apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: amortized-meyceoz

resources:
  - ../../../base

patches:
  - target:
      kind: ConfigMap
      name: amortized-config
    patch: |
      - op: replace
        path: /data/AMORTIZED_COMPUTE_NAMESPACE
        value: "amortized-meyceoz-jobs"
      - op: add
        path: /data/AMORTIZED_IMAGE_PULL_POLICY
        value: "IfNotPresent"
      - op: replace
        path: /data/AMORTIZED_EXTERNAL_URL
        value: "http://amortized-server.amortized-meyceoz.svc.cluster.local:8000"
      - op: replace
        path: /data/AMORTIZED_MLFLOW_TRACKING_URI
        value: "http://mlflow.amortized.svc.cluster.local:5000"
      - op: replace
        path: /data/AMORTIZED_GATEWAY_URL
        value: "http://mlflow.amortized.svc.cluster.local:5000/gateway/mlflow/v1"
      - op: replace
        path: /data/AMORTIZED_S3_BUCKET
        value: "amortized"
      - op: replace
        path: /data/AMORTIZED_DATABASE_URL
        value: "postgresql://amortized:amortized@postgres.amortized.svc.cluster.local:5432/amortized"

  - target:
      kind: Deployment
    patch: |
      - op: add
        path: /spec/template/spec/containers/0/imagePullPolicy
        value: IfNotPresent

  - target:
      kind: Deployment
    patch: |
      - op: replace
        path: /spec/template/spec/securityContext/runAsNonRoot
        value: false

  - target:
      kind: ConfigMap
      name: opencode-config
    patch: |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: opencode-config
      data:
        .opencode.json: |
          {
            "$schema": "https://opencode.ai/config.json",
            "model": "google-vertex-anthropic/claude-opus-4-6@default",
            "mcp": {
              "amortized": {
                "type": "remote",
                "url": "http://amortized-server.amortized-meyceoz.svc.cluster.local:8000/mcp",
                "enabled": true
              }
            },
            "agent": {
              "build": { "disable": true },
              "plan": { "disable": true }
            }
          }

  - target:
      kind: Deployment
      name: claude-code
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: claude-code
      spec:
        template:
          spec:
            containers:
              - name: claude-code
                env:
                  - name: MCP_AMORTIZED_URL
                    value: "http://amortized-server.amortized-meyceoz.svc.cluster.local:8000/mcp"

  - target:
      kind: Deployment
      name: amortized-studio
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: amortized-studio
      spec:
        template:
          spec:
            containers:
              - name: studio
                env:
                  - name: BACKEND_HOST
                    value: "amortized-server.amortized-meyceoz.svc.cluster.local"
                  - name: AGENT_HOST
                    value: "opencode.amortized-meyceoz.svc.cluster.local"
                  - name: MLFLOW_HOST
                    value: "mlflow.amortized.svc.cluster.local"
