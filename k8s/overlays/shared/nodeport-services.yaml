apiVersion: v1
kind: Service
metadata:
  name: mlflow-nodeport
  namespace: amortized
  labels:
    app: amortized
    component: mlflow
spec:
  type: NodePort
  selector:
    app: amortized
    component: mlflow
  ports:
    - port: 5000
      targetPort: http
      nodePort: 31082
