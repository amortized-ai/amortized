{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ServeJobConfig",
  "description": "Configuration for serving a model via vLLM",
  "type": "object",
  "required": ["model_name_or_path"],
  "properties": {
    "model_name_or_path": {
      "type": "string",
      "description": "HuggingFace model ID or local path"
    },
    "adapter_path": {
      "type": "string",
      "description": "Path to LoRA adapter (resolved from artifact)"
    },
    "served_model_name": {
      "type": "string",
      "default": "default",
      "description": "Name for the served model in the API"
    },
    "port": {
      "type": "integer",
      "default": 8000,
      "description": "Port to serve on"
    },
    "tensor_parallel_size": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of GPUs for tensor parallelism"
    }
  },
  "additionalProperties": true
}
