type: training
description: Full SFT — all weights updated via TRL
config:
  algorithm: sft
  num_train_epochs: 1
  learning_rate: 0.00001
  max_length: 4096
  bf16: true
  gradient_checkpointing: true
  report_to: none
