type: training
description: Full SFT — large model fine-tuning with gradient checkpointing
config:
  algorithm: sft
  num_train_epochs: 1
  learning_rate: 0.000005
  max_length: 8192
  bf16: true
  gradient_checkpointing: true
  gradient_accumulation_steps: 8
  report_to: none
