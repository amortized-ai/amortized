type: training
description: OSFT — preserves base capabilities while adapting
config:
  algorithm: osft
  unfreeze_rank_ratio: 0.25
  learning_rate: 5e-6
  max_seq_len: 8192
  effective_batch_size: 128
  lr_scheduler: cosine
  use_liger: true
