# Extraction — SDG Guide

*This guide is a placeholder. It will be populated with expert knowledge
for building entity extraction models.*

In the meantime, use `get_recipes` to find extraction recipes like
`examples/entity-extractor/synth`.
