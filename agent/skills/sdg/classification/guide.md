# Classification — SDG Guide

*This guide is a placeholder. It will be populated with expert knowledge
for building classification models (ticket classifiers, intent routers,
sentiment analysis, content moderation).*

In the meantime, use `get_recipes` to find classification recipes like
`examples/ticket-classifier/synth` or `examples/intent-router/synth`.
