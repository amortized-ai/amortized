type: training
description: DPO — direct preference optimization via TRL
config:
  algorithm: dpo
  num_train_epochs: 1
  learning_rate: 0.000001
  max_length: 1024
  bf16: true
  beta: 0.1
  use_peft: true
  lora_r: 16
  lora_alpha: 32
  report_to: none
