extends: templates/training/lora-sft
description: Qwen3 0.6B — LoRA SFT (fastest, for prototyping)
config:
  model_name_or_path: Qwen/Qwen3-0.6B
  max_length: 2048
  per_device_train_batch_size: 8
  num_train_epochs: 3
  learning_rate: 3.0e-04

compute:
  gpus: 1
  min_vram_gb: 8
