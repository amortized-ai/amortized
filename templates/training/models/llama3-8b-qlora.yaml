extends: templates/training/qlora-sft
description: Llama 3.1 8B Instruct — QLoRA SFT (low VRAM)
config:
  model_name_or_path: meta-llama/Llama-3.1-8B-Instruct
  max_length: 4096
  per_device_train_batch_size: 2
  gradient_checkpointing: true

compute:
  gpus: 1
  min_vram_gb: 12
